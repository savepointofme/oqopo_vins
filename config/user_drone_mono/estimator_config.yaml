%YAML:1.0

# User drone (18r.bag) stereo fisheye + IMU VIO config.
# Based on EuRoC default, tuned for fisheye 1088x880 @ 20 Hz + IMU @ 200 Hz.

verbosity: "INFO"

use_fej: true
integration: "rk4"
use_stereo: false
max_cameras: 1

# Freeze extrinsics/intrinsics/timeoffset: kalibr gave us good values, and
# online calibration on a noisy fisheye with 300s of data tends to drift.
# You can flip these to true to see online-calib effect.
calib_cam_extrinsics: false
calib_cam_intrinsics: false
calib_cam_timeoffset: false
calib_imu_intrinsics: false
calib_imu_g_sensitivity: false

max_clones: 11
max_slam: 50
max_slam_in_update: 25
max_msckf_in_update: 40
dt_slam_delay: 1

# Beijing latitude ~39.6 => g_local ≈ 9.801. Kalibr measured 9.77 on the
# target frame but that includes target-tilt; use WGS84 value for this lat.
gravity_mag: 9.801

feat_rep_msckf: "GLOBAL_3D"
feat_rep_slam: "ANCHORED_MSCKF_INVERSE_DEPTH"
feat_rep_aruco: "ANCHORED_MSCKF_INVERSE_DEPTH"

try_zupt: false
zupt_chi2_multipler: 0
zupt_max_velocity: 0.1
zupt_noise_multiplier: 10
zupt_max_disparity: 0.5
zupt_only_at_beginning: false

# ==================================================================
# Initialization
# ==================================================================

init_window_time: 2.0
init_imu_thresh: 0.3    # much lower: drone takeoff accel is modest
init_max_disparity: 3.0    # fisheye + distant ground = small per-frame disparity
init_max_features: 75

init_dyn_use: true
init_dyn_mle_opt_calib: false
init_dyn_mle_max_iter: 50
init_dyn_mle_max_time: 0.05
init_dyn_mle_max_threads: 6
init_dyn_num_pose: 6
init_dyn_min_deg: 10.0
init_dyn_inflation_ori: 10
init_dyn_inflation_vel: 100
init_dyn_inflation_bg: 10
init_dyn_inflation_ba: 100
init_dyn_min_rec_cond: 1e-12
init_dyn_bias_g: [ 0.0, 0.0, 0.0 ]
init_dyn_bias_a: [ 0.0, 0.0, 0.0 ]

# ==================================================================
# Recording
# ==================================================================

record_timing_information: false
record_timing_filepath: "/tmp/traj_timing.txt"
save_total_state: false
filepath_est: "/tmp/ov_estimate.txt"
filepath_std: "/tmp/ov_estimate_std.txt"
filepath_gt: "/tmp/ov_groundtruth.txt"

# ==================================================================
# Front-end (KLT on 1088x880 fisheye)
# ==================================================================

use_klt: true
num_pts: 200
fast_threshold: 15
grid_x: 5
grid_y: 4
min_px_dist: 12
knn_ratio: 0.70
track_frequency: 20.0
downsample_cameras: false
num_opencv_threads: 4
histogram_method: "HISTOGRAM"

# MSCKF chi2 gating
up_msckf_sigma_px: 1.5
up_msckf_chi2_multipler: 1
up_slam_sigma_px: 1.5
up_slam_chi2_multipler: 1
up_aruco_sigma_px: 1.0
up_aruco_chi2_multipler: 1
use_aruco: false
num_aruco: 1024
downsize_aruco: true

# Mask
use_mask: false

# ==================================================================
# External relative YAMLs
# ==================================================================

relative_config_imu: "kalibr_imu_chain.yaml"
relative_config_imucam: "kalibr_imucam_chain.yaml"
